# Phase D — `attemptMergeOrMove` → `MergeResult` Rewrite — Planning Doc

**Status: draft; D0/D1 done, D2 in progress (15 Aug 2026).** This is the planning pass `specs/BoardStateManager_Extraction_Plan.md` §4 says Phase D needs before any code changes — though D2's first real step (below) means "no code changes" no longer describes this doc's own status, only new-doc-content going forward. §3's design questions: §3.1, §3.2, §3.3, §3.5 (for the producer branch specifically), and §3.6 are now resolved; §3.4 remains open. D0 is complete (`PawSanctuaryTests/AttemptMergeOrMoveCharacterizationTests.swift`, 26 tests). D1 is complete (`PawSanctuary/MergeOutcome.swift` + tests). **D2 has started, one branch at a time rather than the single commit D2's original sketch described** — the producer branch is cut over (`PawSanctuary/MergeResult.swift`, `MergeBoardViewModel.apply(_:)`, see D2's entry below); superpower/merge/swap/move are still inline in `attemptMergeOrMove`, unchanged. Per `CLAUDE.md` rule 5, the producer-branch cutover was done under the user's explicit "keep going with D2" direction — each further branch's cutover is its own step, verified and committed independently, not a license to push the whole rewrite through unsupervised.

**Read `specs/BoardStateManager_Extraction_Plan.md` first.** It covers Phases A–C (state extraction, now fully done) and is the reason Phase D is newly attemptable at all: `BoardStateManager` is dependency-light and unit-testable in a way the rest of `MergeBoardViewModel` isn't.

---

## 1. Current state, measured today

`attemptMergeOrMove(from:to:)` is [MergeBoardViewModel.swift:1762–1905](../PawSanctuary/MergeBoardViewModel.swift#L1762) — **144 lines**, unchanged in size since the Phase A appendix scan (13 Aug), though its absolute line numbers have shifted with later Phase C commits. It is the **only** function in the file that touches all four board-state surfaces (`board[...]`, `boardIsFull`, `emptyUnlockedCells`, `recalcBoardIsFull()`) and it does so through **direct `board[...]` indexing exclusively** — it was explicitly excluded from every Phase C round, so none of the ten `BoardStateManager` primitives are used here yet.

It is not one merge path with options bolted on — it's five structurally distinct outcomes, gated by early returns and nested conditionals, not a shared pipeline:

| Outcome | Lines | What happens |
|---|---|---|
| **Producer merge/swap/move** | 1768–1795 | `srcProducer` present: same-level → upgrade one level (with a 600ms `animatingCell` pulse); different level → swap; empty destination → move. Own `recalcBoardIsFull()`, own early `return`. Explicitly commented as the one exception to the file's "always recalc after an occupancy change" rule (see the comment at 1788–1791) — a real, load-bearing bug-shaped subtlety, not incidental. |
| **Superpower piece spent** | 1801–1812 | `srcItem.chain?.category == .superpower` and a `dstItem` exists → delegates to `applySuperpowerMerge`, which returns a `Bool`. Skips score/XP/bubble/`checkSuperpowerUnlock` entirely — a deliberately separate pipeline from a normal merge. |
| **Wildcard/animal merge** | 1813–1899 | The big one. Eligibility check (wildcard adopts the other item's identity; same-chain-same-tier otherwise) → on success: score (with spotlight multiplier), XP, top-tier celebration *or* bubbling, `mergeCount`/`lastMergeTimestamp`/`lastMergedSpeciesRaw`, `recalcBoardIsFull()`, `updateAllAfterMerge` (→ tier-unlock check, quest/daily-challenge progress ×2 if Hedgehog superpower is active, order progress, spotlight reward payout), `checkSuperpowerUnlock`, `applyPassivePowers`, then **two more branches** for what happens to the merged tile itself: completed sub-object → maybe grants a superpower piece to inventory instead of leaving the tile; power-up-category or completed-sub-object → rolls rarity (if applicable) and moves the tile to power-up inventory. Ends with the 600ms animation pulse and captures `preMoveSnapshot` (Nine Lives undo) — captured **after** all these mutations, deliberately, since assigning it earlier would self-invalidate via `board`'s `didSet` (comment at 1828–1831). |
| **Ineligible pair** | 1896–1899 | Plain item swap, no side effects, no `recalcBoardIsFull()` (occupancy didn't change). |
| **Empty destination** | 1900–1904 | Plain move, `recalcBoardIsFull()`. |

**Side-effect surface reached from the merge branch alone:** `score`, `grantXP`, `mergeCount`, `lastMergeTimestamp`, `lastMergedSpeciesRaw`, `quests` (four distinct call sites across `updateAllAfterMerge`), `adoptionBoardCoordinator` (via `updateOrdersAfterMerge`), `kibbleEngine` (spotlight reward payout), `deepestUnlockedTier`/`checkTierUnlock()`, `unlockedSuperpowerSpecies`/`showSuperpowerUnlockBanner`, `inventoryStore` (power-up/superpower-piece routing), `SoundManager`, `HapticManager`, `enqueueToast`, `animatingCell`, `preMoveSnapshot`. This is not a board-mutation function that happens to also touch a few other things — board mutation is a minority of what it does.

**Zero test coverage.** Confirmed again while writing this doc — `PawSanctuaryTests/` has no `BoardStateManagerTests.swift` (flagged as worth writing in the Phase B entry, never done) and nothing exercises `attemptMergeOrMove` directly. Every verification of merge behavior across Phases B/C was live-simulator testing, never automated.

## 2. Why this needs its own resolution pass, not just "apply the Phase C pattern"

Phase C's primitive-routing pattern (swap `board[pos.row][pos.col].x` for `boardState.x(at: pos)`) doesn't fit here, and applying it directly would be a distraction. The point of `MergeResult` is that this function stops *performing* mutations and side effects inline and instead **computes and returns a description of what should happen**, which something else applies. If that redesign happens, routing the interim direct-`board[...]` reads through primitives first is churn that gets thrown away, not a stepping stone — worth deciding explicitly rather than defaulting into it.

The five-branch shape in §1 also means "rewrite `attemptMergeOrMove`" undersells the task. A `MergeResult` type needs to represent producer upgrades, producer swaps, producer moves, superpower-piece consumption, animal merges (with two further sub-outcomes for where the merged tile ends up), item swaps, and plain moves — seven-ish shapes, not one.

## 3. Open design questions

Two of the six questions below are now **resolved** (15 Aug 2026) — recorded here with rationale rather than left as options, since this doc's job is to be a decision record, not just a list of forks. The other four remain open. Four are genuine decisions this doc isn't the place to unilaterally settle without discussion — the two resolved below turned out to have a forcing fact behind them rather than being purely a matter of taste.

**A forcing fact that shaped both resolutions:** `applyPassivePowers` ([MergeBoardViewModel.swift:2569](../PawSanctuary/MergeBoardViewModel.swift#L2569)) is not deterministic. Fetch!/Multiply/Antler Drop all call `placeFreeTile` ([:2555](../PawSanctuary/MergeBoardViewModel.swift#L2555)), which places an item on `emptyUnlockedCells.randomElement()` — a random cell chosen from the board **as it exists after the primary merge write**, since `recalcBoardIsFull()` has already run by the time `applyPassivePowers` is called. Antler Drop additionally rolls `Int.random(in: 0..<4)`. A "pure `computeMergeResult` → inert data → `apply` just writes it" design (the framing §1/§4 originally implied) is therefore not achievable for the full merge outcome — the passive-power tail depends on post-primary-mutation board state and on randomness, not on anything a pre-mutation compute step could know in advance.

**A second instance of the same fact, found while writing D0's tests:** `maybeBubbleMergedItem` also rolls `Double.random(in: 0..<1) < bubbleChance` (0.15) *after* confirming order/quest eligibility — eligibility is necessary but not sufficient, matching the shape of the passive-power finding above. §1's branch inventory didn't call this out explicitly; it should be read as covered by "maybe bubbles" wherever it appears. This reinforces §3.1's resolution rather than changing it: `computeMergeOutcome` should decide *eligibility* (deterministic, testable), and the actual random roll — like `applyPassivePowers`'s — stays in `apply(_:)`'s procedural tail.

1. **RESOLVED — `MergeResult`'s shape.** Not one monolithic literal-data type. A two-layer split:
   - A pure, testable `computeMergeOutcome(from:to:) -> MergeOutcome?` decides only what's actually deterministic given `from`/`to` and the current board: eligibility (wildcard-adopts-identity vs. same-chain-same-tier), the resulting `chainID`/tier, score delta (with spotlight multiplier), XP delta, whether it's a top-tier completion vs. a bubble candidate, and the merged species. This is the real testable surface, and what D0's characterization tests should target first.
   - `MergeResult` stays a thin `enum`, one case per row of §1's table, carrying only what the deterministic step produced:
     ```swift
     enum MergeResult {
         case producerUpgrade(from: GridPosition, to: GridPosition, newLevel: ProducerLevel)
         case producerSwap(from: GridPosition, to: GridPosition)
         case producerMove(from: GridPosition, to: GridPosition)
         case superpowerPieceSpent(from: GridPosition, at: GridPosition)
         case merge(MergeOutcome)
         case itemSwap(from: GridPosition, to: GridPosition)
         case itemMove(from: GridPosition, to: GridPosition)
         case noOp
     }
     ```
   - `apply(_ result: MergeResult)` stays procedural for the cascade — `updateAllAfterMerge`, `checkSuperpowerUnlock`, `applyPassivePowers`, and the sub-object/power-up completion routing keep running as imperative calls, exactly as today, just centralized under one dispatch point instead of inlined in `attemptMergeOrMove`. This correctly scopes "compute" to the part that's genuinely computable rather than forcing randomized game logic into a data model it doesn't fit — the win is `attemptMergeOrMove` shrinking to "compute, then dispatch" and the eligibility/outcome logic becoming independently testable, not that every last effect becomes inert data.
2. **RESOLVED (implemented in D2's producer-branch step, 15 Aug 2026) — dispatch location.** `apply(_ result: MergeResult)` on `MergeBoardViewModel`, as leaned toward — mirroring `apply(_ s: GameState)`'s existing naming (Swift resolves the overload by parameter type, no naming collision). Lives in `MergeBoardViewModel.swift` itself, right after `attemptMergeOrMove`, not in `MergeResult.swift` — applying a result is genuine stateful ViewModel work, not pure logic, so it stays with the rest of the ViewModel's mutation code rather than living alongside the pure compute functions. The per-sub-coordinator `handle(_:)` alternative wasn't pursued — for the producer branch there's nothing to fan out to (it's board-only), so this didn't get a real test either way. Revisit if the merge branch's cutover (still ahead) makes one `apply` switch statement feel overloaded once it's also dispatching `.merge(MergeOutcome)` cases with their much larger side-effect cascade.
3. **RESOLVED — the Nine Lives snapshot's timing.** The current placement (assigned at the *end* of the merge branch, from values captured at the *start*) exists specifically to dodge `board`'s `didSet`-triggered invalidation, and that constraint is unchanged by the compute/apply split — if anything it gets cleaner to reason about:
   - Capture `preMergeBoard`/`preMergeInventory` **inside `apply(_:)`**, at the top of the `.merge` case, before any write — not inside `computeMergeOutcome`. Snapshot data isn't core merge-outcome data, so it doesn't belong in `MergeOutcome`'s payload; keeping it out also keeps `computeMergeOutcome` genuinely pure (no need to thread board-copy plumbing through a function whose job is just "decide what happens").
   - Assign `self.preMoveSnapshot` as the **literal last statement** of the `.merge` case's handling in `apply(_:)` — after the primary write, the `updateAllAfterMerge`/`checkSuperpowerUnlock` cascade, *and* `applyPassivePowers`'s own board writes. Same ordering guarantee as today (comment at [:1828–1831](../PawSanctuary/MergeBoardViewModel.swift#L1828)), just relocated one level up into `apply(_:)`.
   - Scope stays exactly as narrow as today: only `.merge` does this capture-then-assign dance. Producer moves, swaps, plain moves, and superpower-piece spends don't touch `preMoveSnapshot` currently and shouldn't gain that behavior as an accidental side effect of the rewrite.
4. **Async UI timing isn't result data.** Still open. The 600ms `animatingCell` pulse (`Task { try? await Task.sleep(...); self.animatingCell = nil }`) appears **three** times once `placeFreeTile` is counted (producer upgrade, the merge branch itself, and every `placeFreeTile` call inside `applyPassivePowers`) — and since `animatingCell` is a single property, a passive-power trigger firing `placeFreeTile` during `apply(_:)` will clobber whichever cell the primary merge's own pulse was pointing at. This is a pre-existing quirk of the current code, not something the rewrite introduces, but it's worth deciding whether Phase D leaves it as-is (matching today's actual, slightly-glitchy behavior) or fixes it as a small deliberate scope addition — not as an accidental side effect of restructuring the dispatch.
5. **RESOLVED for the producer branch (D2, 15 Aug 2026); the general question stays open for later branches.** §1 flagged that the producer branch's `recalcBoardIsFull()` timing is deliberately *not* "always recalc" — it's there so a later spawn can overwrite a producer the player just placed. `apply(_:)`'s producer cases preserve this exactly: `recalcBoardIsFull()` and `selectedCell = nil` run unconditionally after the `switch`, including for `.noOp` — ported verbatim, not just conceptually. D0's `testProducerOntoItemOccupiedCellIsNoOp` and the new cross-check tests in `MergeResultTests.swift` are what confirmed this held after the cutover, exactly the safety net this question anticipated needing. Still open for whichever branch's ordering quirks turn up when the merge/superpower/swap/move cases get their own D2 steps — this entry doesn't generalize past the producer branch.
6. **RESOLVED (implemented in D1, 15 Aug 2026) — `computeMergeOutcome`'s parameters.** Explicit parameters, as leaned toward: `computeMergeOutcome(from:to:srcItem:dstItem:spotlightChainID:spotlightMultiplierBonus:orders:urgentOrder:activeQuests:) -> MergeOutcome?`, a free function in `MergeOutcome.swift`, not a `MergeBoardViewModel` method. `ContentRegistry.shared` remains an implicit dependency, same as every other pure helper in this codebase (`isBubbleEligibleForQuestOrOrder`, `spawnCost(forTier:)`) — it's read-only content data, not mutable app state, so it doesn't need threading through. 19 tests in `MergeOutcomeTests.swift` exercise it with zero `MergeBoardViewModel` instantiation, confirming the testability payoff is real — plus two tests that cross-check its output against what `attemptMergeOrMove` actually produces on a live ViewModel, for direct confidence the extraction is faithful rather than just self-consistent.

## 4. Proposed phasing — draft, for discussion

Each phase independently shippable and playable at every commit, per `CLAUDE.md` rule 2 — same discipline B/C already used.

- **D0 — done (15 Aug 2026).** `PawSanctuaryTests/AttemptMergeOrMoveCharacterizationTests.swift`, 26 tests, all against *today's* `attemptMergeOrMove` — pinning current behavior, not a spec of what's "correct." Fixtures build a board directly and assign it to `.board`; `loadGame()` is never called, so tests stay fast and disk-free. Covers all five branches from §1's table: producer merge/swap/move, superpower-piece-spent (consumed and fizzled), the eligible-merge pipeline (score/XP asserted via live `ContentRegistry` lookups rather than hardcoded numbers, wildcard-adopts-identity from both sides, two wildcards never merging), the three swap-fallback reasons (ineligible pair, bubbled destination, already-top-tier), plain moves, and every guard no-op. Also pins the Nine Lives snapshot behavior §3.3 resolved (armed only by a successful merge, captures pre-merge state) and the superpower-unlock trigger at its exact tier.

  **One correction to this doc's own §1, found while writing the tests:** `maybeBubbleMergedItem` rolls `Double.random(in: 0..<1) < bubbleChance` (0.15) even once order/quest eligibility is confirmed — a second gate §1's branch inventory didn't mention. An eligible order makes a bubble *possible*, not guaranteed. The corresponding test is statistical (300 trials) rather than single-shot, matching `QuestCoordinatorTests`' existing convention for this codebase's other RNG-gated systems — verified non-flaky across four separate runs before committing. Anyone building `computeMergeOutcome` (§3.1) needs to carry this gate through explicitly, or the pure step will silently over- or under-bubble relative to today's behavior.

  Full suite: 250/250 passing (224 existing + 26 new). Committed as `8ebf16d`.
- **D1 — done (15 Aug 2026).** `PawSanctuary/MergeOutcome.swift` — `MergeOutcome` (the pure decision's output type) and `computeMergeOutcome(...)` (§3.6, resolved while implementing this). Scoped exactly to §3.1's field list: eligibility (returns `nil` for anything that currently falls back to a swap — ineligible pair, bubbled destination, already top tier), resulting chain/tier, score/XP deltas (spotlight multiplier included), top-tier-completion vs. bubble-*eligible* (not "will bubble" — see §3's bubbleChance note above). Deliberately does **not** decide sub-object/power-up-completion inventory routing or run the passive-power cascade — both stay procedural for now, per the forcing fact. `attemptMergeOrMove` itself is untouched; this is dead code from the app's perspective until D2. `MergeOutcomeTests.swift`: 19 tests, 17 fully isolated (no `MergeBoardViewModel`), 2 cross-checking against the real `attemptMergeOrMove`. Full suite: 269/269. Committed as `13ced65`.

  The full `MergeResult` enum sketched in §3.1 (covering producer/superpower/swap/move, not just `.merge`) wasn't built in D1 — `computeMergeOutcome` returns `MergeOutcome?` directly, matching what the doc's own D1 description named. Building `MergeResult`'s other cases is D2's concern, when `attemptMergeOrMove`'s other branches get their own compute steps and something needs to unify them for dispatch.
- **D2 — in progress, one branch at a time (revised from the original "one commit" sketch).** Each of `attemptMergeOrMove`'s five branches gets its own compute step, `MergeResult` case(s), and cutover commit — verified against D0's characterization tests and live-checked in the simulator before moving to the next, rather than one big-bang commit touching all five at once. This is slower but keeps each step's blast radius small and independently revertable, matching `CLAUDE.md`'s "keep the game playable at every commit" rule more literally than a single 144-line rewrite would.

  - **Producer branch — done (15 Aug 2026, commit `d94b670`).** `PawSanctuary/MergeResult.swift` adds `MergeResult`'s first cases (`producerUpgrade`/`producerSwap`/`producerMove`/`noOp`) and `computeProducerOutcome(...)`, mirroring `computeMergeOutcome`'s shape exactly (explicit parameters, no `MergeBoardViewModel` needed) — except it never returns `nil`, since every input (including a producer dragged onto an item-occupied cell) has a defined outcome, unlike the merge branch's real eligibility question. `attemptMergeOrMove`'s producer branch (18 lines) collapses to three: read the two cells, `apply(computeProducerOutcome(...))`. `apply(_ result: MergeResult)` (§3.2) holds the actual board writes, ported verbatim including the recall-ordering exception (§3.5). 8 new tests in `MergeResultTests.swift`; all four of D0's original producer-branch tests pass unchanged against the new dispatch path — the real proof of faithfulness, not just the new tests being self-consistent. Live-verified: dragging the starter Family Spawner to an empty cell moved it correctly through the new path. Full suite: 277/277.
  - **Superpower/merge/swap/move branches — not started.** The merge branch (wrapping D1's already-built `computeMergeOutcome`) is the natural next step and also the largest — it's the one with the big side-effect cascade §3.2 flagged as the case that might make a single `apply` switch feel overloaded.
- **D3 — cleanup.** Remove the now-dead inline logic left behind by D2, and only *then* consider routing any remaining direct `board[...]` access through `BoardStateManager` primitives, if any survive the rewrite in a form that still needs it.

## 5. Non-goals

This doc does not resolve §3's open questions, does not touch `MergeBoardViewModel.swift` or any other source file, does not change merge rules or economy values, and does not extend to the producer branch's UX. It also doesn't commit to the D0–D3 phasing above as final — that breakdown is a starting proposal for discussion, same as this whole doc.

---

## Appendix — branch inventory (15 August 2026, current line numbers)

Verified by direct reading of `MergeBoardViewModel.swift:1762–1905`, not a scan script (unlike the Phase A appendix, which covered the whole class — this is one function, small enough to read in full and worth re-verifying by eye rather than trusting a regex against its nested-conditional shape).

- **1762–1766:** Guard — `from != to`, destination in bounds, both cells unlocked. Direct `board[...].isUnlocked` reads, ×2.
- **1768–1795:** Producer branch (see §1 table). Direct `board[...].producer` reads/writes throughout. Early `return` at 1794.
- **1797–1798:** Guards — source item exists and isn't bubbled; destination has no producer (producer branch above already returned if it did, but a *different* cell's producer state isn't re-checked without this).
- **1800–1899:** `if let dstItem = board[to.row][to.col].item` — the two occupied-destination outcomes:
  - **1801–1812:** superpower-piece-spent sub-branch, delegates to `applySuperpowerMerge` ([MergeBoardViewModel.swift:2632](../PawSanctuary/MergeBoardViewModel.swift#L2632)).
  - **1813–1895:** eligible-merge sub-branch — the full effect list in §1's table. Calls out to `updateAllAfterMerge` ([:2486](../PawSanctuary/MergeBoardViewModel.swift#L2486)), `checkSuperpowerUnlock` ([:2524](../PawSanctuary/MergeBoardViewModel.swift#L2524)), `applyPassivePowers` ([:2569](../PawSanctuary/MergeBoardViewModel.swift#L2569)), `rollRarityForCompletedSubObject`, `maybeGrantSuperpowerPiece`.
  - **1896–1899:** ineligible-pair else — plain swap.
- **1900–1904:** Empty-destination branch — plain move, `recalcBoardIsFull()`.

**Referenced but out-of-scope for this function's own rewrite** (called into, not migrated as part of Phase D unless a design question in §3 says otherwise): `applySuperpowerMerge`, `updateAllAfterMerge`, `checkSuperpowerUnlock`, `applyPassivePowers`, `updateOrdersAfterMerge`, `maybeBubbleMergedItem`, `triggerTopTierCelebration`, `grantXP`, `checkTierUnlock`.

**`BoardSnapshot`** ([MergeBoardViewModel.swift:99](../PawSanctuary/MergeBoardViewModel.swift#L99)) — `{ board: [[BoardCell]], inventory: [BoardItem?] }`, session-only (not persisted), backs Nine Lives undo. `board`'s computed-property `didSet` ([:143–150](../PawSanctuary/MergeBoardViewModel.swift#L143)) invalidates `preMoveSnapshot` on any board write after it's set — the mechanism §3.3 is about.
